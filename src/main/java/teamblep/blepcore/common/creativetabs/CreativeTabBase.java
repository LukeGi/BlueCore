package teamblep.blepcore.common.creativetabs;

import net.minecraft.creativetab.CreativeTabs;

// TODO: Maybe make this an enum, with each tab as a value
public abstract class CreativeTabBase extends CreativeTabs {

  public CreativeTabBase(String label) {
    super(label);
  }
}
